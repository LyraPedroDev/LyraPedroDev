---
import { t } from '../../i18n/utils';

interface Props {
  lang: 'pt-BR' | 'en';
}

const { lang } = Astro.props;

// URLs das APIs com parâmetros de cores do tema Cyber Dark + card width
const githubStatsUrl = "https://github-readme-stats.vercel.app/api?username=LyraPedroDev&theme=transparent&show_icons=true&bg_color=0D0E16&title_color=7C3AED&icon_color=06B6D4&text_color=E5E7EB&border_color=404060&hide_border=false&card_width=500";
const githubStreakUrl = "https://github-readme-streak-stats.herokuapp.com/?user=LyraPedroDev&theme=dark&background=0D0E16&ring=7C3AED&fire=FF006E&currStreakNum=06B6D4&sideNums=E5E7EB";
const githubLangsUrl = "https://github-readme-stats.vercel.app/api/top-langs?username=LyraPedroDev&theme=transparent&layout=compact&bg_color=0D0E16&title_color=7C3AED&text_color=E5E7EB&border_color=404060&hide_border=false";
---

<section id="github-activity" class="py-24 px-6 relative z-10 max-w-6xl mx-auto fade-in-section">
  <!-- Cabeçalho da Seção -->
  <div class="flex flex-col md:flex-row justify-between items-start md:items-end mb-14 gap-4">
    <div>
      <span class="font-mono text-xs font-bold tracking-widest text-cyber-blue uppercase block mb-2.5">
        {t('github_section_badge', lang)}
      </span>
      <h2 class="font-display text-3xl sm:text-4xl font-extrabold tracking-tight text-white">
        {t('github_section_title', lang)}
      </h2>
    </div>
    <p class="max-w-xs text-gray-400 text-sm leading-relaxed">
      {t('github_section_desc', lang)}
    </p>
  </div>

  <!-- Grid Responsivo (1 col mobile, 2 col tablet, 3 col desktop) -->
  <div class="github-grid">
    <!-- Card 1: Stats Card -->
    <div class="card-container glass-card rounded-2xl overflow-hidden border border-white/5 p-4 flex items-center justify-center transition-all duration-300 hover:border-cyber-purple/30">
      <img
        src={githubStatsUrl}
        alt="GitHub Stats"
        class="w-full h-auto max-w-full rounded-xl shadow-[0_10px_30px_rgba(0,0,0,0.4)]"
        loading="lazy"
        width="450"
        height="270"
      />
    </div>

    <!-- Card 2: Streak Stats -->
    <div class="card-container glass-card rounded-2xl overflow-hidden border border-white/5 p-4 flex items-center justify-center transition-all duration-300 hover:border-cyber-purple/30">
      <img
        src={githubStreakUrl}
        alt="GitHub Streak"
        class="w-full h-auto max-w-full rounded-xl shadow-[0_10px_30px_rgba(0,0,0,0.4)]"
        loading="lazy"
        width="450"
        height="270"
      />
    </div>

    <!-- Card 3: Top Languages (Compact) -->
    <div class="card-container full-width glass-card rounded-2xl overflow-hidden border border-white/5 p-4 flex items-center justify-center transition-all duration-300 hover:border-cyber-purple/30">
      <img
        src={githubLangsUrl}
        alt="Top Languages"
        class="w-full h-auto max-w-full rounded-xl shadow-[0_10px_30px_rgba(0,0,0,0.4)]"
        loading="lazy"
        width="450"
        height="270"
      />
    </div>
  </div>
</section>

<style>
  .github-grid {
    display: grid;
    gap: 1.5rem;
    grid-template-columns: 1fr;
  }

  /* Evita Cumulative Layout Shift (CLS) reservando aspecto 60% */
  .card-container {
    position: relative;
    padding-bottom: 60%;
    overflow: hidden;
    background: rgba(13, 14, 22, 0.55);
    border-radius: 1rem;
    will-change: border-color;
  }

  .card-container img {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    object-fit: fill; /* Mantém proporções completas do SVG */
    padding: 0.5rem;
  }

  @media (min-width: 768px) {
    .github-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    
    /* Na visualização de tablet o 3º card fica embaixo ocupando 2 colunas */
    .card-container.full-width {
      grid-column: 1 / -1;
      padding-bottom: 35%;
    }
  }

  @media (min-width: 1024px) {
    .github-grid {
      grid-template-columns: repeat(3, 1fr);
    }
    
    /* Na visualização de desktop todos os 3 cards ficam na mesma linha */
    .card-container.full-width {
      grid-column: span 1;
      padding-bottom: 60%;
    }
  }
</style>
